# Ray Serve Endpoints Requirements
# Note: Ray and Triton clients are already installed in /opt/ray venv
# This file is for additional dependencies needed by endpoints

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# HTTP client for testing
requests>=2.31.0

# Optional: For Stable Diffusion
# torch>=2.0.0
# transformers>=4.30.0
# diffusers>=0.21.0
# accelerate>=0.20.0
